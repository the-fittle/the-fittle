{
	"name": "docs",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "vite",
		"build": "tsc && vite build",
		"preview": "vite preview",
		"clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
	},
	"dependencies": {
		"ui": "workspace:*"
	},
	"devDependencies": {
		"ts-config": "workspace:*",
		"typescript": "^4.5.3"
	}
}
